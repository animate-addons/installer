{
  "private": true,
  "name": "@animate-addons-installer/app",
  "version": "0.0.1",
  "scripts": {
    "dev": "yarn tauri dev",
    "build": "yarn tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.4.0"
  }
}
